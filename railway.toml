[build]
builder = "DOCKERFILE"
dockerfilePath = "LeninCard/backend/Dockerfile"
buildContext = "."

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
