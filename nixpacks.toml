[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r LeninCard/requirements.txt"]

[phases.build]
cmds = ["echo 'No build step needed'"]

[start]
cmd = "cd LeninCard && uvicorn backend.api.main:app --host 0.0.0.0 --port $PORT --workers 4"
